# Status

## 2026-06-30

Completed first levee-only SAC workflow draft:
- source XAML: `sac/LeveeOnly.LeftBank.xaml`
- current source packet: `output/LeveeOnly.LeftBank.v006.source.pkt`
- validation script: `tests/Validate-LeveeOnly.ps1`

Validated with SAC 2026 runtime objects against a flat preview surface at `Y=-5`:
- `P1 = (0, 0)` origin / wadi crown edge
- `P2 = (-4, 0)` land crown edge
- `P3 = (10, -5)` wadi toe
- `P4 = (-14, -5)` land toe
- `3` links
- `0` runtime errors

Notes:
- This is line-only geometry.
- No shapes are included.
- The generated source `.pkt` is accepted by Autodesk's packet/document reader.
- Final Civil 3D production use still needs SAC 2026 export/compile or a .NET fallback package.

## Sampling Update

Replaced the fixed 10-point sampling prototype:
- packet now includes `AP1` through `AP250` as possible auxiliary sample points
- each sample is guarded by a SAC `FlowDecision`
- sample spacing remains 1 m using `DeltaXOnSurface`
- decision logic uses `ThalwegOffset` when valid, otherwise `MaxScanDistance`
- scan sample autolinks are disabled for now; the sample points are auxiliary only
- `CreateLoop` was not used because SAC loop geometry copies a link vector; it does not re-run `DeltaXOnSurface` on irregular ground

## v009 Packet

Generated Civil 3D-ready packet:
- `output/LeveeOnly.LeftBank.v009.civil3d.pkt`
- includes `.atc`, `.dll`, `.xaml`, `.cfg`, `.emd`, and `.pvd`
- adds `MinMildTrendLength` parameter, default `5 m`
- concave breaks are first stored as candidates, then accepted only after the mild trend continues for `MinMildTrendLength`
- protection links use `Protection`, `ProtectionSteep`, `ProtectionMild`, and `ProtectionMerge` codes
- surface links use `SurfaceLink` code

## v010 Packet

Generated Civil 3D-ready packet:
- `output/LeveeOnly.LeftBank.v010.civil3d.pkt`
- replaces straight sample-to-sample visible ground links with `StripSurfaceActivity` surface strips
- keeps auxiliary `AP1` through `AP250` samples for detection only
- removes old `MinMildLength`
- adds `MildProtectionLength` and `BreakMarkerSize`
- protection start/end points are generated by surface strips at target offsets
- accepted concave breaks and ignored convex breaks get large visible diamond markers
- display codes include `SurfaceYellow` and `ProtectionCyan`; Civil 3D color display still depends on Code Set Style mapping

## v011 Packet

Generated Civil 3D-ready packet:
- `output/LeveeOnly.LeftBank.v011.civil3d.pkt`
- removes `StripSurfaceActivity` from generated XAML to eliminate the suspended horizontal preview line
- removes all convex break output
- removes `GroundSample` point codes so sample points are not styled by code set
- restores visible surface links using normal SAC links between terrain sample points
- keeps concave break diamond markers and protection links

## v012 Packet

Generated Civil 3D-ready packet:
- `output/LeveeOnly.LeftBank.v012.civil3d.pkt`
- restores true `StripSurfaceActivity` surface-following links for yellow ground and cyan protection
- sets strip layout depth to the wadi toe elevation to avoid the old crown-level suspended preview line
- adds fixed toe scour and apron protection from the wadi-side toe
- keeps convex break markers visible for logic checking
- keeps auxiliary sample points uncoded so they are not styled as output points
- skips adaptive concave protection inside the fixed toe scour/apron zone
